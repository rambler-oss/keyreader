// +build freebsd

package main

const (
	configPath = "/usr/local/etc/rambler/keyreader.conf"
	oldgrpPath = "/usr/local/etc/rambler/oldgroup.domains"
	nofullFlag = "/usr/local/etc/rambler/ignorefullaccess"
)
